root = "/app"
tmp_dir = "/app/tmp/controller"

[build]
    cmd = "go build -o /app/tmp/controller/main /app/cmd/controller/"

    entrypoint = "/app/tmp/controller/main"

    include_ext = ["go", "proto", "tmpl", "html"]

    exclude_dir = ["tmp", "vendor", "test", "deployments"]

    delay = 1000

[log]
    time = true
    main_only = true

[color]
    app = "cyan"